# 2026-08-10

## Pi 对接梳理
- 项目中的 "Pi" 指 Pi coding agent（@earendil-works/pi-agent-core，SDK 锁定 0.84.0），非树莓派。
- 实现形态：前端机器工作区 Pi Tab → Server(NestJS 代理, packages/server/src/pi/) → Client(packages/client/src/pi/, fork Worker 加载 Pi SDK) → 远程 ~/.pi/agent。
- 设计文档：docs/remote-pi-tab.md；参考蓝本：examples/pi-web（@agegr/pi-web）。
- Session JSONL 是正文事实来源；Server 只存 sanitized agent.session Job 元数据（jobId === sessionId，每次 prompt 新 runId）。
- 三层均已完成且有测试；未实现：README 中的"客户端 Pi Agent 代理"（Agent 自主执行子任务）。
- 产出 docs/pi-integration-status.md：含 mermaid 架构图/Prompt 时序图/状态机/决策建议图，建议优先级 A（验收固化）→ C（Agent 自主代理）。

## Pi agent 能力调研
- 权威来源：本地包 @earendil-works/pi-coding-agent@0.84.0 自带 docs/（sdk.md、extensions.md、skills.md、packages.md、settings.md、rpc.md 等 30+ 篇）。
- 要点：Pi = pi-ai(LLM 抽象) + pi-agent-core(Agent 循环) + pi-coding-agent(harness: Session/Tools/ResourceLoader/Compaction/Modes) + pi-tui。
- 三种驱动方式：CLI/print、RPC(--mode rpc JSONL)、SDK 嵌入（VCPDeck 已选 SDK+fork Worker，正确）。
- 扩展：extensions（registerTool/25 种事件钩子/registerCommand/ctx.ui/registerProvider）、skills（Agent Skills 标准）、packages（npm/git/local 分发）。
- 自主代理关键能力：SessionManager.inMemory + SettingsManager.inMemory + 独立 agentDir 可无头隔离运行；agent_settled 是完成信号；examples/sdk/12-13 和 examples/extensions/subagent 是蓝本。
- 产出 docs/pi-agent-capabilities.md（含 mermaid 分层图/驱动方式对比/扩展机制图），并在 pi-integration-status.md 头部互链。

## Pi 完全管控方案
- 产出 docs/pi-full-control-design.md：六控制面（C1 会话✅/C2 配置🟡/C3 权限闸门❌/C4 资源分发❌/C5 任务编排❌/C6 观测审计🟡）。
- 关键决策：vcpdeck-gate 扩展经 extensionFactories 注入 Worker 做权限闸门；资源放 ~/.vcpdeck/pi-resources/ 不碰 ~/.pi/agent；任务编排用新 PiTask/PiSubTask 表（不复用 Job）；正文仍只留远程，Server 只回收摘要；任务策略 = 机器策略 ∩ 任务声明。
- 路线：P0 REST 鉴权（硬前置）→ P1 权限闸门 → P2 单机任务编排 → P3 资源分发 → P4 控制台 → P5 多机/定时。
- 现状确认：shared/src/pi.ts 现有 21 个 PiAction（session.* 10 + agent.* 8 + model/thinking/extension）。
