{
  "name": "vcpdeck",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "start": "node scripts/up.cjs",
    "test": "node scripts/test.cjs",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "prisma",
      "esbuild"
    ]
  }
}
